package com.example.transferdata;

import java.net.ServerSocket;
import java.util.concurrent.Executor;

class DeviceInfoServerAsyncTask {
    public DeviceInfoServerAsyncTask(ServerSocket serverSocketDevice, PeersAdapter peersAdapter, Callback callbackReInitDeviceServer) {
    }

    public void executeOnExecutor(Executor threadPoolExecutor) {
    }

    public void cancel(boolean b) {
    }
}
